# The app name shown in the header, page title, and public gallery pages.
# Always sourced from here — never hardcoded in components.
VITE_APP_NAME=PhotoDrop

# Base URL of the backend API (see backend/.env's APP_URL + PORT).
VITE_API_BASE_URL=http://localhost:4000/api
